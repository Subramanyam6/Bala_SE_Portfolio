name: Deploy Backend to Azure App Service

on:
  push:
    branches:
      - main
    paths:
      - 'portfolio-app/backend/**'
      - '.github/workflows/backend-deploy.yml'

permissions:
  id-token: write        # allow azure/login to fetch the OIDC token
  contents: read         # allow checkout to read your code

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Java 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: 'maven'
        
    - name: Build with Maven
      run: cd portfolio-app/backend && mvn clean package -DskipTests
      
    - name: Login to Azure
      uses: azure/login@v2
      with:
        client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_DCA6EC1D9D804AC5AA5FCB6CCB2FA3EA }}
        tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_A73D026242BD43C0A7E338382B24002F }}
        subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_EA2B61CF81B148DEB1DF9CED564654DC }}
      
    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v3
      with:
        app-name: 'portfolio-api-app'
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        package: 'portfolio-app/backend/target/*.jar'
        
    - name: Upload artifact for deployment job
      uses: actions/upload-artifact@v4
      with:
        name: java-app
        path: portfolio-app/backend/target/*.jar 